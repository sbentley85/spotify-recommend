import React from "react";
import { Button, Icon } from "semantic-ui-react";

const AddButton = () => {
	return (
		<Button id="add" icon>
			<Icon name="add" />
		</Button>
	);
};

export default AddButton;
